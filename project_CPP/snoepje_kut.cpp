#include "snoepje_kut.h"

score = score + 10;
fruitx = rand() % WIDTH;
fruity = rand() % HEIGHT;
ntail--;



if(snoepje2 == true)
{
Font::setColor( 0x0B );
cout<<"F";
Font::setColor(0x0F);
}
